---
phase: 13-frontend-discovery-ui-and-cta
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/src/components/aiDiscover/AiSearchInput.tsx
  - frontend/src/components/aiDiscover/AiQuickPrompts.tsx
  - frontend/src/components/aiDiscover/AiExplanation.tsx
  - frontend/src/components/aiDiscover/AiResultsGrid.tsx
  - frontend/src/components/aiDiscover/AiQuickActions.tsx
  - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07]

must_haves:
  truths:
    - "Navigating to /discover/ai shows an idle state with heading, search input, 5 quick-prompt chips, and corpus disclosure"
    - "Tapping a quick-prompt chip auto-submits the query without typing"
    - "While processing, 5 skeleton cards and a skeleton explanation bar appear"
    - "Results appear as MediaCard instances with an AI explanation bubble above"
    - "Refinement buttons generate new queries without clearing the input"
    - "503, 429, 400, and empty results each show distinct error messages"
    - "Unauthenticated users see an auth gate with value-pitch instead of the search UI"
    - "Corpus disclosure is visible on idle state and hidden when results are showing"
  artifacts:
    - path: "frontend/src/pages/aiDiscover/AiDiscoverPage.tsx"
      provides: "Route page with auth guard and all state orchestration"
      min_lines: 80
    - path: "frontend/src/components/aiDiscover/AiSearchInput.tsx"
      provides: "Search input with submit button, 500-char max, char count"
      exports: ["default"]
    - path: "frontend/src/components/aiDiscover/AiQuickPrompts.tsx"
      provides: "5 chip buttons that auto-submit on tap"
      exports: ["default"]
    - path: "frontend/src/components/aiDiscover/AiExplanation.tsx"
      provides: "Accent-bordered blockquote card with sparkle icon"
      exports: ["default"]
    - path: "frontend/src/components/aiDiscover/AiResultsGrid.tsx"
      provides: "Embla horizontal scroll on mobile, 5-col grid on desktop"
      exports: ["default"]
    - path: "frontend/src/components/aiDiscover/AiQuickActions.tsx"
      provides: "Three refinement pill buttons"
      exports: ["default"]
  key_links:
    - from: "frontend/src/pages/aiDiscover/AiDiscoverPage.tsx"
      to: "frontend/src/hooks/aiDiscover/useAiDiscover.ts"
      via: "import useAiDiscover"
      pattern: "import.*useAiDiscover.*from.*hooks/aiDiscover"
    - from: "frontend/src/components/aiDiscover/AiResultsGrid.tsx"
      to: "frontend/src/components/media/cards/MediaCard.tsx"
      via: "import MediaCard"
      pattern: "import.*MediaCard.*from.*media/cards/MediaCard"
    - from: "frontend/src/pages/aiDiscover/AiDiscoverPage.tsx"
      to: "frontend/src/context/SignInModalContext.tsx"
      via: "import useSignInModal"
      pattern: "import.*useSignInModal.*from.*context/SignInModalContext"
---

<objective>
Build the complete AI Discovery page with all components, states, and interactions.

Purpose: This is the core user-facing feature — the page where users submit natural language queries and see AI-ranked movie/TV results. It handles idle, loading, success, empty, and all error states.
Output: AiDiscoverPage route component and 5 child components, fully functional with all state transitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-discovery-ui-and-cta/13-CONTEXT.md
@.planning/phases/13-frontend-discovery-ui-and-cta/13-UI-SPEC.md
@.planning/phases/13-frontend-discovery-ui-and-cta/13-01-SUMMARY.md

<interfaces>
<!-- Types and hooks from Plan 01 -->

From frontend/src/types/aiDiscover.ts:
```typescript
export interface AiDiscoverRequest { query: string; }
export interface AiDiscoverResult {
  tmdbId: number;
  mediaType: "movie" | "tv";
  title: string;
  explanation: string;
  matchScore: number;
}
export interface AiDiscoverResponse {
  results: AiDiscoverResult[];
  message: string;
  responseTimeMs: number;
}
```

From frontend/src/hooks/aiDiscover/useAiDiscover.ts:
```typescript
export function useAiDiscover(): UseMutationResult<AiDiscoverResponse, AxiosError, string>;
// Returns: { mutate, data, isPending, error, reset }
```

From frontend/src/components/media/cards/MediaCard.tsx:
```typescript
type MediaCardProps = {
  id: number;
  media_type: MediaType;
  title: string;
  posterPath: string;
  overview?: string;
  releaseDate?: string;
  vote_average?: number;
  genre_ids?: number[];
  vote_count?: number;
  original_language?: string;
  runtime?: number;
  number_of_seasons?: number;
  number_of_episodes?: number;
  disableHoverModal?: boolean;
  showQuickAdd?: boolean;
  onClick?: () => void;
};
```

From frontend/src/components/media/skeleton/MediaCardSkeleton.tsx:
```typescript
export default function MediaCardSkeleton(): JSX.Element;
```

From frontend/src/context/SignInModalContext.tsx:
```typescript
export function useSignInModal(): {
  isOpen: boolean;
  openSignInModal: (callback?: () => void) => void;
  closeSignInModal: () => void;
  pendingCallback: (() => void) | null;
};
```

From frontend/src/hooks/user/useUser.ts:
```typescript
export function useUser(): UseQueryResult<User | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI Discovery child components</name>
  <files>
    frontend/src/components/aiDiscover/AiSearchInput.tsx,
    frontend/src/components/aiDiscover/AiQuickPrompts.tsx,
    frontend/src/components/aiDiscover/AiExplanation.tsx,
    frontend/src/components/aiDiscover/AiResultsGrid.tsx,
    frontend/src/components/aiDiscover/AiQuickActions.tsx
  </files>
  <read_first>
    - frontend/src/components/media/cards/MediaCard.tsx (props interface, import path)
    - frontend/src/components/media/skeleton/MediaCardSkeleton.tsx (import path, usage)
    - frontend/src/components/media/carousel/CollectionCarousel.tsx (Embla horizontal scroll pattern with flex-[0_0_calc(50%-12px)] slides)
    - frontend/src/style/tokens.css (CSS variable names: --accent-primary, --component-primary, --input, --subtle, --action-primary, --action-hover, --border, --outline, --ring)
    - frontend/src/types/aiDiscover.ts (types from Plan 01)
    - frontend/src/index.css (existing keyframes: shimmer, fadeIn)
  </read_first>
  <action>
    Create directory `frontend/src/components/aiDiscover/`.

    **AiSearchInput.tsx** — Props: `{ query: string; onQueryChange: (q: string) => void; onSubmit: () => void; isPending: boolean }`.
    - Single-line `<input type="text">` with `maxLength={500}`, placeholder `"A movie where a guy relives the same day..."`.
    - Background: `bg-[var(--input)]`, focus ring: `focus:ring-2 focus:ring-[var(--ring)]`, rounded: `rounded-xl`, padding: `px-4 py-3`, full width.
    - Submit button on right: `<button>` inside a flex row with the input. Text: "Search for movies" when idle, "Searching..." when `isPending`. Disabled when `isPending` or `query.trim().length === 0`.
    - Button style: `bg-[var(--action-primary)] hover:bg-[var(--action-hover)] text-white font-semibold text-sm rounded-xl px-4 py-3 min-h-11 transition-colors duration-200`. When disabled: `opacity-50 cursor-not-allowed`.
    - Enter key submits (call `onSubmit`), but only if not pending and query is non-empty.
    - Character count: show `"{N} characters left"` as `text-sm text-[var(--subtle)]` below the input when remaining chars < 100. Calculate as `500 - query.length`.
    - When `isPending`: input gets `opacity-50 cursor-not-allowed` and is `disabled`.

    **AiQuickPrompts.tsx** — Props: `{ onSelect: (prompt: string) => void; visible: boolean }`.
    - Renders 5 hardcoded prompt strings as pill buttons in a `flex flex-wrap gap-2` container.
    - Prompts: "A mind-bending thriller like Inception", "Cozy feel-good movie for a rainy day", "Something with a twist ending", "A movie where the villain wins", "Dark comedy about ordinary people".
    - Each chip: `<button>` with `border border-[var(--outline)] bg-[var(--action-primary)] hover:border-[var(--accent-primary)]/75 hover:bg-[var(--action-hover)] text-white text-sm font-semibold rounded-full px-4 py-2 min-h-11 transition-colors duration-200`.
    - On click: call `onSelect(promptText)`.
    - When `visible` is false: render `null`.

    **AiExplanation.tsx** — Props: `{ message: string }`.
    - Accent-bordered blockquote card per D-04.
    - Container: `border-l-4 border-[var(--accent-primary)] bg-[var(--component-primary)] rounded-r-xl px-4 py-3`.
    - Header row: sparkle icon (Lucide `Sparkles` from `lucide-react`, size 16, `text-[var(--accent-primary)]`) + "AI Discovery" label (`text-sm font-semibold`) in a `flex items-center gap-2`.
    - Body: `<p className="text-base text-white mt-2">{message}</p>`.

    **AiResultsGrid.tsx** — Props: `{ results: AiDiscoverResult[] }`.
    - Import `MediaCard` from `../../media/cards/MediaCard` and `AiDiscoverResult` type from `../../../types/aiDiscover`.
    - On mobile (default): Embla horizontal scroll carousel. Use `useEmblaCarousel` with `{ loop: false, align: "start", dragFree: false }`. Viewport div with `overflow-hidden` and ref `emblaRef`. Inner container `flex gap-6`. Each slide: `<div className="flex-[0_0_calc(50%-12px)] min-w-0">`. This matches the CollectionCarousel pattern per D-05 and the 03-01 decision.
    - On desktop (`lg:` breakpoint): CSS grid `lg:grid lg:grid-cols-5 lg:gap-4`. Hide Embla container on lg and show grid instead. Implementation: render two containers — one with Embla (visible by default, `lg:hidden`) and one with grid (`hidden lg:grid lg:grid-cols-5 lg:gap-4`).
    - Each result maps to `<MediaCard id={r.tmdbId} media_type={r.mediaType} title={r.title} posterPath="" />`. Note: the API response does not include posterPath — MediaCard will show the fallback. The `posterPath` will need to be resolved. Since the backend does not return poster paths, pass `posterPath=""` and `vote_average={r.matchScore * 10}` to show match score via the existing `RatingPill` on desktop hover (per D-06). Set `disableHoverModal={false}` for desktop hover behavior.
    - Actually, MediaCard needs a poster to look right. The tmdbId + mediaType is enough for MediaCard's Link to work. For the poster, we need TMDB image. Since the backend only returns tmdbId, the component should fetch poster data. However, the simplest approach: pass empty posterPath and let the Poster component handle it (it already shows a placeholder). This is acceptable for MVP — per D-07, cards show "title, year, and poster only (standard MediaCard behavior)" but the API doesn't return poster_path. Accept this limitation and note it.

    **AiQuickActions.tsx** — Props: `{ onRefine: (action: string) => void; currentQuery: string; resultTitles: string[] }`.
    - Three pill buttons in a `flex flex-wrap gap-2` container.
    - Buttons: "More like these", "TV shows instead", "Something darker".
    - Same styling as AiQuickPrompts chips.
    - On click, generate a new query string based on the action:
      - "More like these": `"More movies like " + resultTitles.slice(0, 3).join(", ")`
      - "TV shows instead": `currentQuery + " (TV shows only)"`
      - "Something darker": `"Something darker: " + currentQuery`
    - Call `onRefine(generatedQuery)`.
  </action>
  <verify>
    <automated>cd /home/faivern/streaming-app/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/aiDiscover/AiSearchInput.tsx contains `maxLength={500}`
    - frontend/src/components/aiDiscover/AiSearchInput.tsx contains `"Search for movies"` and `"Searching..."`
    - frontend/src/components/aiDiscover/AiSearchInput.tsx contains `characters left`
    - frontend/src/components/aiDiscover/AiQuickPrompts.tsx contains `"A mind-bending thriller like Inception"`
    - frontend/src/components/aiDiscover/AiQuickPrompts.tsx contains `"Dark comedy about ordinary people"`
    - frontend/src/components/aiDiscover/AiExplanation.tsx contains `border-l-4` and `border-[var(--accent-primary)]`
    - frontend/src/components/aiDiscover/AiExplanation.tsx contains `"AI Discovery"`
    - frontend/src/components/aiDiscover/AiExplanation.tsx contains `Sparkles` (Lucide icon import)
    - frontend/src/components/aiDiscover/AiResultsGrid.tsx contains `useEmblaCarousel`
    - frontend/src/components/aiDiscover/AiResultsGrid.tsx contains `flex-[0_0_calc(50%-12px)]`
    - frontend/src/components/aiDiscover/AiResultsGrid.tsx contains `lg:grid-cols-5`
    - frontend/src/components/aiDiscover/AiResultsGrid.tsx contains `import.*MediaCard`
    - frontend/src/components/aiDiscover/AiQuickActions.tsx contains `"More like these"`
    - frontend/src/components/aiDiscover/AiQuickActions.tsx contains `"TV shows instead"`
    - frontend/src/components/aiDiscover/AiQuickActions.tsx contains `"Something darker"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 5 AI discover child components created with correct props, styling, and behavior per UI-SPEC and CONTEXT.md decisions</done>
</task>

<task type="auto">
  <name>Task 2: Create AiDiscoverPage with all state orchestration</name>
  <files>frontend/src/pages/aiDiscover/AiDiscoverPage.tsx</files>
  <read_first>
    - frontend/src/pages/home/HomePage.tsx (existing page component pattern, lazy export)
    - frontend/src/hooks/user/useUser.ts (auth check pattern)
    - frontend/src/context/SignInModalContext.tsx (openSignInModal usage)
    - frontend/src/hooks/aiDiscover/useAiDiscover.ts (hook from Plan 01)
    - frontend/src/components/aiDiscover/AiSearchInput.tsx (props interface from Task 1)
    - frontend/src/components/aiDiscover/AiQuickPrompts.tsx (props interface from Task 1)
    - frontend/src/components/aiDiscover/AiExplanation.tsx (props interface from Task 1)
    - frontend/src/components/aiDiscover/AiResultsGrid.tsx (props interface from Task 1)
    - frontend/src/components/aiDiscover/AiQuickActions.tsx (props interface from Task 1)
    - frontend/src/components/media/skeleton/MediaCardSkeleton.tsx (loading skeleton)
  </read_first>
  <action>
    Create `frontend/src/pages/aiDiscover/AiDiscoverPage.tsx` as a default export (required for lazy loading).

    **State management:**
    - `const [query, setQuery] = useState("")` — search input text
    - `const { mutate, data, isPending, error, reset } = useAiDiscover()`
    - `const { data: user } = useUser()`
    - `const { openSignInModal } = useSignInModal()`
    - `const [hasSubmitted, setHasSubmitted] = useState(false)` — tracks whether a query has been submitted (used to toggle idle/results view)

    **Submit handler:**
    ```typescript
    const handleSubmit = () => {
      if (!query.trim() || isPending) return;
      setHasSubmitted(true);
      mutate(query.trim());
    };
    ```

    **Quick prompt handler:**
    ```typescript
    const handleQuickPrompt = (prompt: string) => {
      setQuery(prompt);
      setHasSubmitted(true);
      mutate(prompt);
    };
    ```

    **Refinement handler:**
    ```typescript
    const handleRefine = (newQuery: string) => {
      setQuery(newQuery);
      mutate(newQuery);
    };
    ```

    **Error state derivation** (from AxiosError):
    ```typescript
    const errorStatus = error?.response?.status;
    const retryAfterSeconds = error?.response?.headers?.["retry-after"]
      ? parseInt(error.response.headers["retry-after"], 10)
      : null;
    ```

    **Layout structure** — page container: `<div className="min-h-dvh bg-background mt-navbar-offset pb-bottom-nav md:pb-0 px-page">`.

    **Auth gate (unauthenticated):** When `!user`, render a centered auth gate instead of the search UI:
    - Container: `flex flex-col items-center justify-center` with generous top padding (`pt-16`).
    - Heading: `<h1 className="text-2xl sm:text-3xl font-semibold text-white text-center">Discover movies with AI</h1>`
    - Body: `<p className="text-base text-[var(--subtle)] text-center mt-4 max-w-md">Sign in to describe what you're in the mood for and get personalized recommendations powered by AI.</p>`
    - CTA button: `<button onClick={() => openSignInModal()} className="mt-6 bg-[var(--action-primary)] hover:bg-[var(--action-hover)] text-white font-semibold rounded-xl px-6 py-3 min-h-11 transition-colors duration-200">Sign in to get started</button>`
    - Per D-11, this calls `openSignInModal()` which triggers the existing `SignInModal`.

    **Idle state (authenticated, !hasSubmitted):**
    - Centered layout with `flex flex-col items-center text-center pt-16`.
    - Heading: `<h1 className="text-2xl sm:text-3xl font-semibold text-white">What are you looking for?</h1>`
    - Subheading: `<p className="text-base text-[var(--subtle)] mt-4 max-w-lg">Describe a mood, a scene you remember, or what you're in the mood for.</p>`
    - `<AiSearchInput>` with `mt-6`, `max-w-2xl w-full`.
    - `<AiQuickPrompts visible={true} onSelect={handleQuickPrompt} />` with `mt-4`.
    - Corpus disclosure: `<p className="text-sm text-[var(--subtle)] mt-3">Searching across 10,000 popular titles</p>` — per D-02.

    **Results state (authenticated, hasSubmitted):**
    - Top-aligned layout (not centered).
    - `<AiSearchInput>` at top with `mt-6 max-w-2xl w-full mx-auto`.
    - Below input, full-width results area with `mt-6` spacing:
      - If `isPending`: show explanation skeleton bar (`<div className="border-l-4 border-[var(--accent-primary)] bg-[var(--component-primary)] rounded-r-xl px-4 py-3 animate-pulse h-20" />`) + 5 `<MediaCardSkeleton>` in a flex row (`flex gap-6 overflow-hidden`) or grid on desktop (`lg:grid lg:grid-cols-5 lg:gap-4`).
      - If `error`: render error state based on `errorStatus`:
        - 503: heading "AI is temporarily unavailable", body "Our discovery service is down. Please try again in a moment."
        - 429: heading "You've reached the query limit", body "Try again in {N} minutes." where N = `Math.ceil((retryAfterSeconds || 30) / 60)`. If retryAfterSeconds is available, show a countdown.
        - 400: heading "Invalid query", body "Your query couldn't be processed. Keep it under 500 characters and describe a movie or show."
        - Default/other: heading "Something went wrong", body "Please try again."
        - Error card container: `bg-[var(--component-primary)] rounded-xl px-6 py-8 text-center mt-6`.
        - Heading: `text-xl font-semibold text-white`, body: `text-base text-[var(--subtle)] mt-2`.
      - If `data && data.results.length === 0`: empty state card with heading "No matches found", body "Try different words or describe the mood you're going for." Same card styling as error states.
      - If `data && data.results.length > 0`:
        - `<AiExplanation message={data.message} />` with `mt-6`.
        - `<AiResultsGrid results={data.results} />` with `mt-6`.
        - `<AiQuickActions onRefine={handleRefine} currentQuery={query} resultTitles={data.results.map(r => r.title)} />` with `mt-4`.

    Quick-prompt chips: hidden after first submission (per discretion — `visible={!hasSubmitted}`). Corpus disclosure: hidden when `hasSubmitted` (per D-02).
  </action>
  <verify>
    <automated>cd /home/faivern/streaming-app/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `export default`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `useAiDiscover`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `useUser`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `useSignInModal`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `"What are you looking for?"`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `"Searching across 10,000 popular titles"`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `"Discover movies with AI"`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `"Sign in to get started"`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `"AI is temporarily unavailable"`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `"You've reached the query limit"`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `"No matches found"`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `"Invalid query"`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `mt-navbar-offset`
    - frontend/src/pages/aiDiscover/AiDiscoverPage.tsx contains `MediaCardSkeleton`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AiDiscoverPage renders auth gate for unauthenticated users, idle state with heading/input/chips/disclosure for authenticated users, and loading/success/empty/error states after submission — all copy matches UI-SPEC copywriting contract</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 6 files exist at their expected paths
- Page handles all 6 states: auth gate, idle, loading, success, empty, error (503/429/400)
- All copy matches the Copywriting Contract in UI-SPEC
- Embla carousel used for mobile results, CSS grid for desktop
- Quick-prompt chips use the 5 specified prompts
</verification>

<success_criteria>
- AiDiscoverPage orchestrates all state transitions correctly
- Auth gate shows value-pitch and triggers SignInModal for unauthenticated users
- 5 child components render with correct styling per UI-SPEC color/typography/spacing
- Error states show distinct messages per HTTP status code
- Results use MediaCard with Embla scroll on mobile, 5-col grid on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-discovery-ui-and-cta/13-02-SUMMARY.md`
</output>
