---
phase: 12-rag-query-service-and-api
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RAG-04, PERS-02]
gap_closure: true

must_haves:
  truths:
    - "ROADMAP Success Criterion 3 reflects D-11 override: 503 on LLM failure instead of vector fallback"
    - "ROADMAP Success Criterion 2 reflects D-07 override: watched-title filtering only, no genre boosting"
    - "REQUIREMENTS.md RAG-04 status reflects descoped-to-503 behavior, not falsely marked Complete"
    - "REQUIREMENTS.md PERS-02 is moved out of Phase 12 scope"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected success criteria for Phase 12"
      contains: "503"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected requirement statuses"
      contains: "Descoped"
  key_links: []
---

<objective>
Align ROADMAP.md and REQUIREMENTS.md with the actual Phase 12 implementation by correcting two documentation misalignments identified in VERIFICATION.md.

Purpose: The code is correct per design decisions D-07 and D-11, but the planning documents incorrectly describe success criteria and requirement statuses that do not match the shipped behavior. This causes verification failures that are false negatives.
Output: Updated ROADMAP.md (SC-2 and SC-3) and REQUIREMENTS.md (RAG-04 and PERS-02 statuses).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-rag-query-service-and-api/12-CONTEXT.md
@.planning/phases/12-rag-query-service-and-api/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 12 Success Criteria 2 and 3</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/12-rag-query-service-and-api/12-CONTEXT.md
    - .planning/phases/12-rag-query-service-and-api/12-VERIFICATION.md
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` in the Phase 12 Success Criteria section (lines 87-92).

**SC-2 (line 88)** — Current text:
```
  2. A user with 20 watched titles receives results that contain none of those titles; preferred genres appear higher in the ranking than unpreferred genres
```
Replace with:
```
  2. A user with 20 watched titles receives results that contain none of those titles (genre boosting deferred per D-07 — watched-title filtering only in Phase 12)
```

**SC-3 (line 89)** — Current text:
```
  3. When the LLM call fails or returns malformed JSON, the endpoint returns vector similarity results rather than a 503 error
```
Replace with:
```
  3. When the LLM call fails or returns malformed JSON, the endpoint returns HTTP 503 with Retry-After header (per D-11 — clean unavailability signal preferred over degraded vector-only results)
```

Also update the Phase 12 Requirements line (line 85). Current text:
```
**Requirements**: RAG-01, RAG-02, RAG-03, RAG-04, RAG-05, PERS-01, PERS-02, GUARD-01, GUARD-02, GUARD-03
```
Replace with:
```
**Requirements**: RAG-01, RAG-02, RAG-03, RAG-05, PERS-01, GUARD-01, GUARD-02, GUARD-03 (RAG-04 descoped per D-11; PERS-02 deferred per D-07)
```
  </action>
  <verify>
    <automated>grep -n "D-11" /home/faivern/streaming-app/.planning/ROADMAP.md && grep -n "D-07" /home/faivern/streaming-app/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md SC-2 contains "genre boosting deferred per D-07"
    - .planning/ROADMAP.md SC-2 does NOT contain "preferred genres appear higher in the ranking"
    - .planning/ROADMAP.md SC-3 contains "HTTP 503 with Retry-After header (per D-11"
    - .planning/ROADMAP.md SC-3 does NOT contain "returns vector similarity results rather than a 503 error"
    - .planning/ROADMAP.md Requirements line contains "RAG-04 descoped per D-11" and "PERS-02 deferred per D-07"
    - grep "D-11" .planning/ROADMAP.md returns at least 2 matches (SC-3 and Requirements line)
    - grep "D-07" .planning/ROADMAP.md returns at least 2 matches (SC-2 and Requirements line)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 12 success criteria and requirements line accurately reflect shipped behavior per D-07 and D-11</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md RAG-04 and PERS-02 statuses</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/12-rag-query-service-and-api/12-CONTEXT.md
    - .planning/phases/12-rag-query-service-and-api/12-VERIFICATION.md
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` to fix two requirement statuses.

**1. RAG-04 (line 22)** — Current text:
```
- [x] **RAG-04**: Pipeline falls back to raw vector results when LLM call fails
```
Replace with:
```
- [~] **RAG-04**: ~~Pipeline falls back to raw vector results when LLM call fails~~ Descoped (D-11): LLM failure returns 503 instead of degraded vector-only results
```

**2. PERS-02 (line 38)** — Current text:
```
- [ ] **PERS-02**: User's preferred genres and cast boosted in LLM ranking prompt
```
Replace with:
```
- [ ] **PERS-02**: User's preferred genres and cast boosted in LLM ranking prompt (deferred from Phase 12 per D-07; future phase)
```

**3. Traceability table — RAG-04 (line 96)** — Current text:
```
| RAG-04 | Phase 12 | Complete |
```
Replace with:
```
| RAG-04 | Phase 12 | Descoped (D-11) |
```

**4. Traceability table — PERS-02 (line 99)** — Current text:
```
| PERS-02 | Phase 12 | Pending |
```
Replace with:
```
| PERS-02 | Deferred | Deferred (D-07) |
```

**5. Update the coverage count (line 114-116):**
Change:
```
- v2.0 requirements: 23 total
- Mapped to phases: 23
- Unmapped: 0
```
To:
```
- v2.0 requirements: 23 total
- Mapped to phases: 22
- Descoped: 1 (RAG-04 per D-11)
- Unmapped: 0
```
  </action>
  <verify>
    <automated>grep -n "RAG-04" /home/faivern/streaming-app/.planning/REQUIREMENTS.md && grep -n "PERS-02" /home/faivern/streaming-app/.planning/REQUIREMENTS.md && grep -n "Descoped" /home/faivern/streaming-app/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md RAG-04 line contains "Descoped (D-11)" and NOT "[x]" as its checkbox
    - .planning/REQUIREMENTS.md PERS-02 line contains "deferred from Phase 12 per D-07"
    - .planning/REQUIREMENTS.md traceability table RAG-04 row shows "Descoped (D-11)" not "Complete"
    - .planning/REQUIREMENTS.md traceability table PERS-02 row shows "Deferred (D-07)" not "Pending"
    - .planning/REQUIREMENTS.md coverage section shows "Descoped: 1 (RAG-04 per D-11)"
    - grep "Descoped" .planning/REQUIREMENTS.md returns at least 2 matches (requirement line and traceability row)
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately reflects RAG-04 as descoped and PERS-02 as deferred, with traceability table and coverage counts updated</done>
</task>

</tasks>

<verification>
- grep "D-11" .planning/ROADMAP.md returns matches in SC-3 and Requirements line
- grep "D-07" .planning/ROADMAP.md returns matches in SC-2 and Requirements line
- grep "Descoped" .planning/REQUIREMENTS.md returns matches for RAG-04
- grep "Deferred" .planning/REQUIREMENTS.md returns matches for PERS-02
- No SC in ROADMAP.md references "vector similarity results rather than a 503 error"
- No SC in ROADMAP.md references "preferred genres appear higher in the ranking than unpreferred genres"
- RAG-04 is NOT marked [x] (Complete) in REQUIREMENTS.md
</verification>

<success_criteria>
- ROADMAP.md Phase 12 SC-2 and SC-3 match the actual shipped behavior
- REQUIREMENTS.md RAG-04 status is Descoped (D-11), not Complete
- REQUIREMENTS.md PERS-02 is marked as deferred from Phase 12
- Traceability table is consistent with requirement statuses
- Both gaps from VERIFICATION.md are resolved by documentation alignment
</success_criteria>

<output>
After completion, create `.planning/phases/12-rag-query-service-and-api/12-04-SUMMARY.md`
</output>
